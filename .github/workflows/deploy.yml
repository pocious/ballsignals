name: Deploy to Hostinger

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 82.198.227.180
          port: 65002
          username: u343042962
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/u343042962/domains/ballsignals.com/public_html
            git pull origin main
            composer install --no-dev --optimize-autoloader -q
            php artisan optimize:clear
            php artisan migrate --force
            mkdir -p storage/app/public/news
            ln -sfn /home/u343042962/domains/ballsignals.com/public_html/storage/app/public public/storage
            php artisan news:fetch-football
